package org.example.menu;

import org.openqa.selenium.WebDriver;

public class MenuPrincipalCompra {
    private WebDriver driver;

    public MenuPrincipalCompra(WebDriver driver) {
        this.driver = driver;
    }
}
